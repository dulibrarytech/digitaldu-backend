var config = require('../config/config'),
    request = require('request');

exports.getMets = function (folder, callback) {

    'use strict';

    // TODO: parse out file names (master and derivative)
};